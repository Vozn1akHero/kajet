import React, { memo }  from 'react'

import "./footer.scss"

const Footer = () => {
    return (
        <footer className="footer">
            <span>&#9400; Vozn1akHero</span>
        </footer>
    );
};

export default memo(Footer)
